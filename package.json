{
  "dependencies": {
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.4.1",
    "@commitlint/config-conventional": "^19.4.1",
    "husky": "^9.0.11"
  },
  "lint-staged": {
    "**/*.ts": [
      "prettier --ignore-path ./.prettierignore --write"
    ],
    "**/*.json": [
      "prettier --ignore-path ./.prettierignore --write"
    ]
  },
  "scripts": {
    "prepare": "husky"
  }
}
